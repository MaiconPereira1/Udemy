/*Valor primitivo são valores imutáveis e ou copiados ex: String, number, boolean, undefind, null(bigint, symble)

let nome = 'Luiz';
nome[0] = 'R';
console.log(nome[0]);  => nesse exemplo o indice L nao pode ser
 alterado para R, por isso é imutavel  
*/

/**
 * Referências (mutaveis) e ou passados por referência- Arrays, object, function
 *  
  let a = [1, 2, 3,] ;
  let b = a;
  console.log(a, b);

  a.push(4);
  console.log(a, b);  =>push foi usado para alterar, adicionando
   o 4 na array, logo é mutável 
 */
